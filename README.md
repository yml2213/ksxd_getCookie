# KSXD Cookie Tool

一个基于 Electron 的桌面工具，用于打开快手小店登录页并在登录成功后提取 Cookie。

## 开发环境

```bash
npm install
npm start
```

## 打包 Windows EXE

```bash
npm run build:win
```

如果在 macOS 上打包 Windows 安装包失败，通常需要额外安装 Wine。
